@using Sistrategia.Drive.WebSite.Models
@model HomeDetailViewModel
@{
  ViewBag.Title = "Detail";
}

<h2>@Model.DriveItem.Name</h2>

<div>
  @if (Model.IsImage) {
    <img src="@Model.GetTempUrl()" style="max-width:720px;max-height:720px;" />
  }
  else {
    @*<p>For download click <a download='@Model.CloudStorageItem.FileName' href="@Model.Url">here.</a></p>*@
    <p>For download click <a download="Test.docx" href="@Model.GetTempDownloadUrl()">here.</a></p>
  }
</div>

